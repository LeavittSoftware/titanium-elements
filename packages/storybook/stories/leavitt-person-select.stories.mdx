import { Story, Canvas, Meta, ArgsTable } from '@storybook/addon-docs';
import { html } from 'lit';

import '@leavittsoftware/leavitt-elements/lib/leavitt-person-select';
import '@leavittsoftware/user-manager';
import ApiService from '@leavittsoftware/api-service/lib/api-service';
import { AuthenticatedTokenProvider } from '@leavittsoftware/api-service/lib/authenticated-token-provider';
import { isDevelopment } from '@leavittsoftware/titanium-helpers/lib/titanium-dev-detection';

<Meta title="UI LEAVITT SPECIFIC/leavitt-person-select" parameters={{ component: 'leavitt-person-select' }} />

# Leavitt Person Select

Leavitt specific person single select element

## How to use

```bash
npm i @leavittsoftware/leavitt-elements
```

```js
import '@leavittsoftware/leavitt-elements/lib/leavitt-person-select';
```

## API

<ArgsTable of="leavitt-person-select" />

## Demo

<Canvas withToolbar>
  <Story name="Docs">
    {html` <h2 demo>Standard</h2>
      <leavitt-person-select
        style="width:350px"
        .apiService=${(() => {
          const api3UserService = new ApiService(new AuthenticatedTokenProvider());
          api3UserService.baseUrl = isDevelopment ? 'https://devapi3.leavitt.com/' : 'https://api3.leavitt.com/';
          api3UserService.addHeader('X-LGAppName', 'DocumentsAdmin');
          return api3UserService;
        })()}
        @selected=${e => {
          if (e.target?.selected) {
            console.log(e.target.selected);
          }
        }}
      ></leavitt-person-select>
      <h2 demo>Shaped</h2>
      <leavitt-person-select
        shaped
        .apiService=${(() => {
          const api3UserService = new ApiService(new AuthenticatedTokenProvider());
          api3UserService.baseUrl = isDevelopment ? 'https://devapi3.leavitt.com/' : 'https://api3.leavitt.com/';
          api3UserService.addHeader('X-LGAppName', 'DocumentsAdmin');
          return api3UserService;
        })()}
        @selected=${e => {
          console.log(e.target.selected);
        }}
      ></leavitt-person-select>
      <h2 demo>Shallow</h2>
      <leavitt-person-select
        shallow
        .apiService=${(() => {
          const api3UserService = new ApiService(new AuthenticatedTokenProvider());
          api3UserService.baseUrl = isDevelopment ? 'https://devapi3.leavitt.com/' : 'https://api3.leavitt.com/';
          api3UserService.addHeader('X-LGAppName', 'DocumentsAdmin');
          return api3UserService;
        })()}
        @selected=${e => {
          console.log(e.target.selected);
        }}
      ></leavitt-person-select>
      <h2 demo>Required</h2>
      <leavitt-person-select
        required
        .apiService=${(() => {
          const api3UserService = new ApiService(new AuthenticatedTokenProvider());
          api3UserService.baseUrl = isDevelopment ? 'https://devapi3.leavitt.com/' : 'https://api3.leavitt.com/';
          api3UserService.addHeader('X-LGAppName', 'DocumentsAdmin');
          return api3UserService;
        })()}
        @selected=${e => {
          console.log(e.target.selected);
        }}
      ></leavitt-person-select>
      <h2 demo>Selected</h2>
      <leavitt-person-select
        .apiService=${(() => {
          const api3UserService = new ApiService(new AuthenticatedTokenProvider());
          api3UserService.baseUrl = isDevelopment ? 'https://devapi3.leavitt.com/' : 'https://api3.leavitt.com/';
          api3UserService.addHeader('X-LGAppName', 'DocumentsAdmin');
          return api3UserService;
        })()}
        @selected=${e => {
          console.log(e.target.selected);
        }}
        .selected=${{ Id: 11056, FirstName: 'Aaron', LastName: 'Drabeck' }}
        required
      ></leavitt-person-select>
      <h2 demo>Custom OData</h2>
      <leavitt-person-select
        .odataParts=${['top=8', 'orderby=FirstName', 'select=FirstName,LastName,CompanyName,Id,Biography', 'count=true', 'filter=Biography ne null']}
        .apiService=${(() => {
          const api3UserService = new ApiService(new AuthenticatedTokenProvider());
          api3UserService.baseUrl = isDevelopment ? 'https://devapi3.leavitt.com/' : 'https://api3.leavitt.com/';
          api3UserService.addHeader('X-LGAppName', 'DocumentsAdmin');
          return api3UserService;
        })()}
        @selected=${e => {
          console.log(e.target.selected);
        }}
      ></leavitt-person-select>
      <h2 demo>Disabled</h2>
      <leavitt-person-select disabled></leavitt-person-select>`}
  </Story>
</Canvas>
