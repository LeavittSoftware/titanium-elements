import { Story, Canvas, Meta, ArgsTable } from '@storybook/addon-docs';
import { html } from 'lit';

import '@leavittsoftware/titanium-select';

<Meta title="UI COMPONENTS/titanium-select" parameters={{ component: 'titanium-select' }} />

# Titanium Select

Material design inspired select. This component is just a wrapper for a native select adding styles and functionality.

## How to use

```bash
npm i @leavittsoftware/titanium-select
```

```js
import '@leavittsoftware/titanium-select';
```

## API

<ArgsTable of="titanium-select" />

## Demo

<Canvas withToolbar>
  <Story name="Docs">
    {() => {
      const options = [
        { Name: 'Saurischia', Id: 1 },
        { Name: 'Sauropodomorpha', Id: 2 },
      ];
      return html`
        <titanium-select
          @value-changed=${event => {
            console.log(event.detail);
          }}
          .options=${options}
          label="Taxonomy"
        ></titanium-select>
      `;
    }}
  </Story>
</Canvas>
