import { Story, Canvas, Meta, ArgsTable } from '@storybook/addon-docs';
import { html } from 'lit';
import { PRIMARY_STORY } from '@storybook/addon-docs';
import '@leavittsoftware/titanium-button';

<Meta
  title="UI COMPONENTS/titanium-button"
  argTypes={{
    styles: {
      table: {
        disable: true,
      },
    },
    click: {
      action: 'Clicked!',
      table: {
        disable: true,
      },
    },
  }}
  parameters={{ component: 'titanium-button' }}
/>

# Titanium Button

Material design inspired button.

## How to use

```bash
npm i @leavittsoftware/titanium-button
```

```js
import '@leavittsoftware/titanium-button';
```

## API

<ArgsTable story={PRIMARY_STORY} />

## Demo

export const TitaniumButtonStory = ({ data, ...args }) =>
  html`
    <h3>Standard</h3>
    <titanium-button
      ?shaped=${args['shaped']}
      ?dense=${args['dense']}
      ?outlined=${args['outlined']}
      ?disabled=${args['disabled']}
      ?unelevated=${args['unelevated']}
      ?raised=${args['raised']}
      @click=${args.click}
      >APPLY</titanium-button
    >
    <h3>Long button text</h3>
    <titanium-button style="width:200px;" unelevated>Are you sure you want to continue?</titanium-button>
  `;

<Canvas withToolbar>
  <Story name="Docs">{TitaniumButtonStory.bind({})}</Story>
</Canvas>
