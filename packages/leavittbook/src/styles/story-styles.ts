import { css } from 'lit';

const StoryStyles = css`
  titanium-card {
    margin-bottom: 36px;
  }

  h1 {
    margin-bottom: 0;
  }

  p {
    margin-bottom: 24px;
    font-size: 13px;
    color: var(--app-light-text-color);
  }
`;
export default StoryStyles;
