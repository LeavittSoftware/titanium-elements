import { AuthZeroLgUserManager } from '@leavittsoftware/web/leavitt/user-manager/auth-zero-lg-user-manager';

const UserManager = new AuthZeroLgUserManager();
Object.freeze(UserManager);
export default UserManager;
